extends layout

block content
  h1.text-center= title
  ul
    each album, i in albums
      each user, i in fakeUsers
        if album.userId == user.id
          h3= `Album ${album.id}: ${album.title} by ${user.name}`
          a(href='/albums/' + album.id + '/photos') View photos..
          hr